#include<stdio.h>

int main()
{
    int A[10];
    int i,low,high,L,H,temp;
    printf("Enter the data element of an array");
    for(i=0;i<10;i++)
    {
        scanf("%d",&A[i]);
    }
    void quick_sort(int low,int high)
    
}